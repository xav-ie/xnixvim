{ ... }:
{
  # TODO: consider barbar.enable instead
  # tabs
  plugins.bufferline = {
    enable = true;
    separatorStyle = "thin";
    showBufferCloseIcons = false;
    indicator.icon = "▌";
    tabSize = 0;
  };
}
