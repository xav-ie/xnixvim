{ ... }:
{
  config = {
    # pretty notifications
    # https://github.com/rcarriga/nvim-notify
    plugins.notify = {
      enable = true;
      backgroundColour = "#00000000";
      fps = 60;
    };
  };
}
